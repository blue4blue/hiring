# SQL and Data Modelling

* Write a query that sessionises events with a 30-minute inactivity gap.
* Compute a user's state on a given historical date from a change-log table.
* How do you handle a dimension that changes over time when you need point-in-time joins?
* What tests do you write on a data model, and why those?
* A model started producing 20% more rows after a deploy. Walk me through your diagnosis.
* When do you materialise a table versus leave it as a view?
* How do you plan a backfill on a table that downstream dashboards depend on?
* What is a semantic layer for, and what problem does it actually solve?
* Describe a data model you designed. What would you change now?

## Follow-ups to test depth

* "What is the cost of that query, and could you halve it?"
* "What breaks if an event arrives three days late?"
